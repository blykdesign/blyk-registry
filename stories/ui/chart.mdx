import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks'
import * as Stories from './chart.stories'
import { DocDivider } from '../doc-display'

<Meta of={Stories} />

# Chart

Charts built with Recharts. Wraps Recharts components with theme-aware colors and tooltips.

## Installation

```bash
npx shadcn@latest add "https://registry.blyk.design/r/base/chart.json"
```

## Usage

```tsx
import { ChartContainer, ChartTooltip, ChartTooltipContent } from "@/components/ui/chart"
```

```tsx
<ChartContainer config={chartConfig}>
  {/* Recharts components */}
</ChartContainer>
```

<DocDivider />

## Examples

### Bar Chart Example

<Canvas of={Stories.BarChartExample} />

<DocDivider />

## API Reference

<ArgTypes of={Stories} />
